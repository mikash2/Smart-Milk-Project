# Open a TCP listener on port 1883 for any container to connect
listener 1883 0.0.0.0
protocol mqtt

# Development only: allow connections without a username/password
allow_anonymous true

# Optional: keep messages between restarts
persistence true
persistence_location /mosquitto/data/

